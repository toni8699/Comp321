# Validate header: N (1..1e6) and k (1..1e18), separated by a single space.
INT(1,1000000,N) SPACE INT(1,1000000,Q) NEWLINE

# Validate the line containing N pile sizes gi, each in [1, 1e9],
# separated by single spaces and terminated by a newline.
INT(1,1000000000) {SPACE INT(1,1000000000)}* NEWLINE

# Validate the line containing Q query values kj, each in [1, sum gi],
# separated by single spaces and terminated by a newline.
INT(1,1000000000000000000) {SPACE INT(1,1000000000000000000)}* NEWLINE

# No trailing tokens or blank lines are allowed.
EOF
